services:
  - type: web
    name: whatsapp-bot
    runtime: node
    buildCommand: npm install && npx puppeteer browsers install chrome
    startCommand: npm start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
